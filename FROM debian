FROM debian 

ENV PYTHONUNBUFFERED-1 

#You can RUN things, mostly to install dependecies 
RUN apt-get update && apt-get upgrade -y && \ 
    apt-get install python3 python3-pip -yes && \ 
    pip3 install pandas && \
    pip3 install -i https://pypi.org/project/lambdata-noah40povis/0.0.7/ lambdata-noah40povis && \
    python -c ïmport lambdata_noah40povis; print(lambdata_noah40povis.TEST)"